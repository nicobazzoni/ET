


<h1> Claim a Planet </h1>
<%= form_for(@alien_planet) do |f| %>
    <%=f.label :name%> <br>
    <%= f.text_field :name %><br>
    <%=f.label :species %><br>
    <%=f.collection_select :species, Species.all, :id, :name, prompt: true %><br>
    <%= f.submit "Submit" %>
    <% end %>
  
    
  